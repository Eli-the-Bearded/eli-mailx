Prints the first few lines of each message (including headers). Has become
less useful as mail systems expand the number of headers on a typical
message. The `toplines` setting will change the number printed. See "summary".
