Standard behavior moves read messages in the main mailbox to ~/mbox. This
can be used to override that selectively; the `hold` variable will override
that universally. Also called "preserve". See also "mbox".
